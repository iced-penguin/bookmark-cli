[package]
name = "bookmark-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "bm"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5.17", features = ["derive"] }
dialoguer = { version = "0.11.0", features = ["fuzzy-select"] }
console = "0.15.8"

[dev-dependencies]
rstest = "0.22.0"
